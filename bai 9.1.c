#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a=2;
	printf("day so chan tu 0 den 100:\n");
	for(a=2;a<=100;a+=2)
	printf("%d\n",a);
	return 0;
}
